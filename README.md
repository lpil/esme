# esme

```sh
gleam run                # Run the project
gleam test               # Run the tests
esbuild --bundle build/dev/javascript/esme/gleam.main.mjs --platform=node --target=node12 > dist/esme.js
```
